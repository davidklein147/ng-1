import { DisplayDataPipe } from './display-data.pipe';

describe('DisplayDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDataPipe();
    expect(pipe).toBeTruthy();
  });
});
